version: 1
AppDir:
  path: ./AppDir
  app_info:
    path: ./AppRun
    exec: usr/bin/Bonsai
    exec_args: $APPIMAGE
  files:
    - from: .
      to: .
      includes:
        - usr/**
  mime_types: []
AppImage:
  arch: x86_64
